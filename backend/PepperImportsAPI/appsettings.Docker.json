{
  // As variáveis sensíveis (ConnectionString, Jwt:Key) são passadas
  // via variáveis de ambiente no docker-compose — NÃO aqui.
  // Este arquivo apenas configura comportamentos específicos do Docker.
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
